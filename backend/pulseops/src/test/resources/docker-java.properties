# Docker Engine 29+ rejects the API version Testcontainers' docker-java client asks for by default (1.32).
# Pinning a supported version keeps the integration tests working on current Docker Desktop.
api.version=1.44
